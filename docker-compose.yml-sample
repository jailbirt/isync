version: '2'

services:
    isync:
        image: bcouto/isync 
        environment:
            - SYNC_INTERVAL=300
            - SYNC_CONFIG=/mail/mbsync.conf
            - CHANNEL=work
        volumes:
            - ./mail/:/mail/
